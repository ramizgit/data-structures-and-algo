BFS: Finds shortest paths in an unweighted graph.
Dijkstra: Finds shortest paths from a single source in a graph with non-negative edge weights.
Bellman-Ford: Finds shortest paths from a single source, supports negative weights, and detects negative cycles.
Floyd-Warshall: Finds shortest paths between every pair of vertices by treating each vertex as a possible intermediate, supports negative weights, and detects negative cycles.


| Edge Weights               | Algorithm                     |
| -------------------------- | ----------------------------- |
| All edges = 1 (unweighted) | ✅ BFS                         |
| Weights ∈ {0,1}            | ✅ 0-1 BFS                     |
| Weights ≥ 0 & single source| ✅ Dijkstra                    |
| Negative weights exist     | ❌ Dijkstra → use Bellman-Ford |
| All-pairs shortest path    | Can use Floyd–Warshall if n is small, otherwise Dijkstra |


The choice of algorithm depends on edge weights, not on whether the graph is directed or undirected.

Dijkstra vs Floyd–Warshall
| Feature                      | Dijkstra                                | Floyd–Warshall                      |
| ---------------------------- | --------------------------------------- | ----------------------------------- |
| **Problem Type**             | Single-source shortest path             | All-pairs shortest path             |
| **Approach**                 | Greedy                                  | Dynamic Programming                 |
| **Core Idea**                | Always pick nearest unvisited node      | Try every node as intermediate      |
| **Time Complexity**          | `O(E log V)` (with heap)                | `O(V³)`                             |
| **Space Complexity**         | `O(V)`                                  | `O(V²)`                             |
| **Graph Type**               | Works best for sparse graphs            | Works fine for dense graphs         |
| **Data Structure**           | Priority Queue (Min Heap)               | 2D matrix                           |
| **Example Use Case**         | GPS routing from one city               | Network latency between all servers |


Shortest parth algos:-
| Algorithm               | Graph Type         | Negative Edges            | Negative Cycle | Source    | Time                 | Space    | Typical Use                             |
| ----------------------- | ------------------ | ------------------------- | -------------- | --------- | -------------------- | -------- | --------------------------------------- |
| **BFS**                 | Unweighted         | N/A                       | N/A            | Single    | **O(V + E)**         | O(V)     | Shortest path in unweighted graph       |
| **0-1 BFS**             | Edge weights {0,1} | No                        | No             | Single    | **O(V + E)**         | O(V)     | Binary weighted graphs                  |
| **Dijkstra**            | Weighted           | ❌ No                      | ❌ No           | Single    | **O((V + E) log V)** | O(V + E) | Shortest path with non-negative weights |
| **Bellman-Ford**        | Weighted           | ✅ Yes                     | ✅ Detects      | Single    | **O(V × E)**         | O(V)     | Negative edges / cycle detection        |
| **Floyd-Warshall**      | Weighted           | ✅ Yes                     | ✅ Detects      | All pairs | **O(V³)**            | O(V²)    | All-pairs shortest paths                |
| **Johnson's Algorithm** | Weighted           | ✅ Yes (no negative cycle) | Detects        | All pairs | **O(VE + V² log V)** | O(V²)    | Sparse graph all-pairs shortest paths   |

